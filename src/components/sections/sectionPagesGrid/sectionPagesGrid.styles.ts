import { SxProps } from "@mui/material";

export const sectionPagesGridStyles: SxProps = {
  ".actions-container": {
    position: "relative",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  },
};
